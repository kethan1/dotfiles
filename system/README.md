This folder contains configuration files that should be placed in "system" locations, like `/etc`. Given that writing to these locations usually requires root access and has security concerns, I recommend copying the files over rather than symlinking them with stow.

